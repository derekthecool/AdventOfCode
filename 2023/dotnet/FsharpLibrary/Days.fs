namespace Days

open Helpers.AdventDataFetcher

module Answers =
    let Day1 =
        let tracer = (Tracer()).DoTrace()
        printfn "tracer: %A" tracer

    let Day2 =
        let tracer = (Tracer()).DoTrace()
        printfn "tracer: %A" tracer
